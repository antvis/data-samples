
import * as data from './multi_series_line_chart-test-data.json'

import type { DataSample } from '../../interface';

const dataSample: DataSample = {
  name: 'multi_series_line_chart-test-data',
  data,
};

export default dataSample;
