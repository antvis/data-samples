import * as data from './density_stack_chart-test-data.json';

import type { DataSample } from '../../interface';

const dataSample: DataSample = {
  name: 'density_stack_chart-test-data',
  data,
};

export default dataSample;
