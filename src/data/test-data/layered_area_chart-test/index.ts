import * as data from './layered_area_chart-test-data.json';

import type { DataSample } from '../../interface';

const dataSample: DataSample = {
  name: 'layered_area_chart-test-data',
  data,
};

export default dataSample;
