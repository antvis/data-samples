import * as data from './trellis_stacked_bar_chart-test-data.json';

import type { DataSample } from '../../interface';

const dataSample: DataSample = {
  name: 'trellis_stacked_bar_chart-test-data',
  data,
};

export default dataSample;
