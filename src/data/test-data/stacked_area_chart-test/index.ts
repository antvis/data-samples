
import * as data from './stacked_area_chart-test-data.json'

import type { DataSample } from '../../interface';

const dataSample: DataSample = {
  name: 'stacked_area_chart-test-data',
  data,
};

export default dataSample;
